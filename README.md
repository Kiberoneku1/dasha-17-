##мой прэкт .сайт с мазгами на Python

сайт умеет рекомендовать фильмы и игры по вводу настроения пользователя.

###Стек
мы используем Html, css,Python.Руализован фулстак сайт, с созданием лакального сервера и подключением нейросети к сайту
используются библиотеки Flask, trasformrrs, pipeline/

...
   pip install Flask
   pip install trasformrrs
   pip install pipeline
   pip install tarch

